# Local Vite development can use the built-in http://localhost:4000 fallback.
# Production and Vercel must set the public HTTPS URL of the backend service.
VITE_API_BASE_URL=https://your-backend-service.example.com
